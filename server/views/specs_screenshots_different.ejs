<div class="modal-header">
  <button type="button" class="close" data-dismiss="modal" aria-hidden="true">&times;</button>
  <h3>Spec images</h3>
</div>
<div class="modal-body" style="max-height: none;">
  <p>The spec screenshot didn't match the reference one. If you consider that the new one should be the reference, click on "Change reference image"</p>
  <img src="<%= expectedImage %>" title="Reference image" height="80%">
  <img src="<%= actualImage %>" title="Actual image" height="80%">
  <p style= "margin-top: 10px;">
    <a href="#" class="btn" onclick="$(\"#screenshots_comparison_<%= specsSuiteId %>\").remove();">Cancel</a>
    <a href="#" class="btn btn-primary" onclick="changeSpecScreenshot(<%= specsSuiteId %>, <%= specId %>);">Change reference image</a>
  </p>
</div>
